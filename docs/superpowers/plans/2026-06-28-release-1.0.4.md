# Bump Version to 1.0.4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump the project version to 1.0.4, synchronize manifests, commit with the required attribution trailer, and push tags to release.

**Architecture:** We use npm's versioning command to bump package.json and trigger sync-manifest-version.mjs to update the Claude-plugin manifests, then manually commit and tag to handle the required Co-Authored-By attribution trailer cleanly on Windows.

**Tech Stack:** npm, Node.js, Git

## Global Constraints

- Commit attribution: Every commit must end with the Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com> trailer.
- No legacy-compatibility shims.

---

### Task 1: Cleanliness Check and Validation

**Files:**
- Modify: None
- Test: None

**Interfaces:**
- Consumes: Clean working directory
- Produces: Successful validation and test pass

- [ ] **Step 1: Check git status to ensure working directory is clean**

Run: `git status`
Expected: "nothing to commit, working tree clean"

- [ ] **Step 2: Run npm test to verify all tests pass**

Run: `npm test`
Expected: Exit code 0, all tests pass (validate, test:node, test:python)

---

### Task 2: Bump version and sync manifests

**Files:**
- Modify: `package.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`
- Test: None

**Interfaces:**
- Consumes: Standard npm version bump command
- Produces: Files updated with version 1.0.4 and staged in Git

- [ ] **Step 1: Run npm version to update version to 1.0.4 without creating a git commit/tag automatically**

Run: `npm version 1.0.4 --no-git-tag-version`
Expected: `package.json` version updated to `1.0.4`, and `bin/sync-manifest-version.mjs` runs automatically to update `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` and stages them.

- [ ] **Step 2: Verify git status shows modified and staged files**

Run: `git status`
Expected: `package.json`, `.claude-plugin/plugin.json`, and `.claude-plugin/marketplace.json` are modified and staged.

---

### Task 3: Commit and tag with attribution

**Files:**
- Modify: None
- Test: None

**Interfaces:**
- Consumes: Staged files from Task 2
- Produces: Commit and tag created with proper attribution

- [ ] **Step 1: Commit the staged changes with the required attribution**

Run: `git commit -m "1.0.4" -m "Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>"`
Expected: Commit created successfully.

- [ ] **Step 2: Create the git tag v1.0.4 pointing to the new commit**

Run: `git tag v1.0.4`
Expected: Tag v1.0.4 created.

- [ ] **Step 3: Push the commit and tags to the remote repository**

Run: `git push && git push --tags`
Expected: Pushes succeed.
